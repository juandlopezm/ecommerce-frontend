server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta del cliente (/producto/:id, /admin, /carrito…) sirve index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
